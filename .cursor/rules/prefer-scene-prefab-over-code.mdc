---
description: Prefer scene/prefab property fixes over code; user edits existing scene
alwaysApply: true
---

# 优先场景 / 预制体，少改代码

用户明确要求（冲突时以本文件为准）：

**凡是改场景（含 Main.scene）或改预制体、给组件暴露 `@property` 再在编辑器里拖绑就能解决的问题，不要改业务逻辑代码。**

## 谁改场景

1. **已有场景内容**（Main 里已有节点/已绑属性）：AI **禁止直接改** `Main.scene`。只给用户**详细步骤**（哪个节点、哪个组件、改哪个属性、拖哪个引用）。
2. **场景里新加的物品**（本次新建并放进场景的 prefab/节点）：AI **可以**改场景里与这些新物品相关的部分。
3. 新建/改 `assets/resources/prefabs/**`、脚本上**只加** `@property` 供拖绑：可以。

## 做法

1. 错绑 `towerId` / 漏勾 `isExpandTower` / `dropMin`/`dropMax` / Label 未绑 / 列表未拖墙 → **不要改玩法代码**；已有节点则写清让用户在编辑器改。
2. 需要新接口时：脚本上 **只加** `@property`（或空列表），说明绑什么；不要顺手改玩法逻辑。
3. 仅当场景/预制体无法表达（算法错误、事件漏发、系统边界违反等）时才改业务代码。
4. 交付用白话：节点路径 → 组件 → 属性名 → 应填/应拖的值。

## 与既有硬约束

- 仍遵守塔防工作流：默认不做 Main 场景编排；本规则进一步限制「已有内容由用户改」。
- 「暴露属性 + 用户绑」优先于「脚本运行时纠偏」（例如不要运行时强行改写错误的 `towerId`）。
