---
description: 双 AI 协作：开工先读 HANDOFF.md 与 AGENTS.md，按分工与交接协议执行
alwaysApply: true
---

# Codex ↔ Cursor 协作约定（强制）

1. 开工前先读 `AGENTS.md` 与 `docs/HANDOFF.md`，并用 git 确认最新状态（pull）。
2. 分工：Cursor 负责 prefab / 场景 / 绑定说明；Codex 负责 `assets/scripts` 业务逻辑与 git。详见 `AGENTS.md` 第三节。
3. 收工必须 commit 并更新 `docs/HANDOFF.md`（已完成 / 下一步 / 需用户绑定 / 坑），**禁止留未提交改动给对方**。
4. 若 `docs/HANDOFF.md` 中已有"请 Cursor 做 X"，本会话优先处理该任务；已有"请 Codex 做 X"时不要抢先实现。
5. 同一时间只改自己负责的文件域；需要对方配合时写进 HANDOFF，不要直接改对方文件。
6. 需求先对照 `assets/塔防AI工作流.md` 与 `.cursor/rules/tower-defense-workflow.mdc`，冲突以 MD 为准。
