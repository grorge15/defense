import { _decorator, Component } from 'cc';

const { ccclass } = _decorator;

/**
 * @deprecated 提示逻辑已整合到 VirtualJoystick，此组件保留为空壳避免序列化报错。
 */
@ccclass('JoystickHintUI')
export class JoystickHintUI extends Component {}
